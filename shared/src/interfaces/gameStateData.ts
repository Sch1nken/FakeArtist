export interface IGameStateData {
    // TODO: Fill
    // This is used to update clients with only the relevant gamestate data
    // Clients don't need to know about sockets or who is the leader/fake artist etc
    a: string;
}